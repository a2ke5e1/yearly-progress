<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YearlyProgress" parent="Theme.Material3Expressive.DynamicColors.DayNight.NoActionBar" />
    <!--
    Having themes.xml for night-v31 because of the priority order of the resource qualifiers.
    -->
    <style name="Theme.YearlyProgress.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="appWidgetRadius">@android:dimen/system_app_widget_background_radius</item>
        <item name="appWidgetInnerRadius">@android:dimen/system_app_widget_inner_radius</item>
        <item name="android:backgroundTint">@android:color/system_neutral2_800</item>
    </style>
</resources>